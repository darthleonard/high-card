import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darthleonard.highcard',
  appName: 'high-card',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
